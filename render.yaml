# Render Blueprint — one-click deploy for CAMPUSCONNECT
# Docs: https://render.com/docs/blueprint-spec
#
# How to use:
#   1. Push this repo to GitHub (git push)
#   2. On render.com → New → Blueprint → pick this repo
#   3. Fill in the env vars it asks for (DATABASE_URL, JWT_SECRET, …)
#   4. Deploy — every future `git push` auto-redeploys the app
#
# The build installs all deps, creates the DB tables (prisma db push),
# builds the React client, and the server serves API + client on one port.

services:
  - type: web
    name: campusconnect
    runtime: node
    plan: free
    buildCommand: npm run install:all && npm run db:push --prefix server && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Paste your Neon connection string here, e.g.
      # postgresql://user:pass@ep-xxx.region.aws.neon.tech/campusconnect?sslmode=require
      - key: DATABASE_URL
        sync: false
      # Generate one with: openssl rand -hex 32
      - key: JWT_SECRET
        sync: false
      # Optional — leave blank to use offline AI fallbacks
      - key: GEMINI_API_KEY
        sync: false
      # Your live URL, e.g. https://campusconnect.onrender.com
      - key: PUBLIC_URL
        sync: false
