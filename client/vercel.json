{
  "outputDirectory": "dist",
  "installCommand": "npm install && cd api && npm install && npx prisma generate",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
